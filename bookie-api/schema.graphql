type User {
    id: Int!
    email: String!
    name: String!
    unitSize: Float!
    updatedAt: String!
    createdAt: String!
}

type Session {
    token: String!
    user: User!
}

type Query {
    login(email: String!, password: String!): Session!
    myUserInfo: User!
    hello: String!
}

type Mutation {
    register(email: String!, password: String!, name: String!, unitSize: Float!): Session!
    changePassword(oldPassword: String!, newPassword: String!): Boolean!
}
